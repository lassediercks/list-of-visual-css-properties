#List of visual css properties

A list of css properties that influence the first visual impression.

e.g. `color` `background-color`

Properties that don't influence the visual behaviour are things like 

`position` or `cursor`